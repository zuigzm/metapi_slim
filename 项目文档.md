# Metapi 项目文档

> **版本**: 1.3.0 | **许可证**: MIT | **语言**: TypeScript (全栈)

Metapi 是一个 **AI 中转站聚合网关**——将多个分散的 AI 中转站（New API、One API、OneHub、DoneHub、Veloera、AnyRouter、Sub2API 等）汇聚为一个统一的 API 入口，提供自动模型发现、智能路由和成本优化能力。

---

## 目录

- [项目概述](#项目概述)
- [技术栈](#技术栈)
- [目录结构](#目录结构)
- [架构分层](#架构分层)
- [核心模块](#核心模块)
- [支持的平台](#支持的平台)
- [快速开始](#快速开始)
- [部署方式](#部署方式)

---

## 项目概述

当前 AI 生态中存在大量基于 New API / One API 系列的聚合中转站，管理多个站点的余额、模型列表和 API 密钥既分散又耗时。Metapi 作为这些中转站之上的**元聚合层（Meta-Aggregation Layer）**，提供：

- **统一入口**：一个 API Key + 一个端点，对接所有下游工具（Cursor、Claude Code、Codex、Open WebUI 等）
- **自动发现**：自动探测上游站点可用的模型列表
- **智能路由**：根据模型、成本、健康状态自动选择最优通道
- **多协议支持**：兼容 OpenAI、Anthropic、Google Gemini、Codex 等协议
- **账户管理**：支持 OAuth 登录、自动签到、余额跟踪
- **可视化管理**：Web 管理面板 + Electron 桌面应用

---

## 技术栈

| 层级 | 技术 |
|------|------|
| **运行时** | Node.js >= 25，TypeScript 6.x |
| **后端框架** | Fastify 5.x |
| **前端框架** | React 18.x + React Router 7.x |
| **前端构建** | Vite 6.x + Tailwind CSS 4.x |
| **数据库 ORM** | Drizzle ORM 0.45.x |
| **数据库支持** | SQLite（默认）、MySQL、PostgreSQL |
| **桌面端** | Electron (builder: electron-builder) |
| **文档站** | VitePress |
| **测试** | Vitest |
| **包管理** | npm / pnpm |
| **容器化** | Docker + Docker Compose |
| **部署平台** | Render、Zeabur、K3s |

---

## 目录结构

```
metapi/
├── src/
│   ├── server/              # 后端服务 (Fastify)
│   │   ├── index.ts         # 服务入口，注册路由和调度器
│   │   ├── config.ts        # 配置解析 (环境变量)
│   │   ├── contracts/       # 路由请求/响应 payload 类型定义
│   │   ├── routes/
│   │   │   ├── api/         # 管理 API 路由 (账户、令牌、站点、统计等)
│   │   │   └── proxy/       # AI 代理路由 (chat/completions/embeddings/models 等)
│   │   ├── proxy-core/      # 代理编排核心逻辑
│   │   │   ├── orchestration/  # 端点流程编排 (endpointFlow)
│   │   │   ├── conductors/     # 代理执行器、重试策略、流终止
│   │   │   ├── executors/      # 协议执行器 (Claude/Codex/Gemini CLI)
│   │   │   ├── providers/      # 供应商配置和注册表
│   │   │   ├── surfaces/       # API 协议表面层
│   │   │   ├── runtime/        # 运行时支持 (WebSocket/会话)
│   │   │   ├── capabilities/   # 功能特性检测
│   │   │   └── cliProfiles/    # CLI 工具兼容配置 (Claude Code/Codex/Gemini)
│   │   ├── transformers/   # 协议请求/响应转换器 (纯函数)
│   │   │   ├── openai/     # OpenAI 协议 (chat, responses)
│   │   │   ├── anthropic/  # Anthropic 协议 (messages)
│   │   │   ├── gemini/     # Gemini 协议 (generate-content)
│   │   │   ├── canonical/  # 标准格式
│   │   │   └── shared/     # 共享转换工具
│   │   ├── services/       # 业务逻辑层
│   │   │   ├── platforms/  # 上游平台适配器
│   │   │   ├── oauth/      # OAuth 认证与登录
│   │   │   ├── shared/     # 共享业务工具
│   │   │   └── *.ts        # 核心服务 (Model/TokenRouter/Proxy 等)
│   │   ├── middleware/     # 中间件 (认证、限流)
│   │   ├── db/             # 数据库层 (schema、migration、runtime bootstrap)
│   │   ├── shared/         # 服务端共享工具
│   │   ├── test-fixtures/  # 测试夹具
│   │   └── update-helper/  # 更新中心辅助服务
│   │
│   ├── web/                # 前端 SPA (React + Vite)
│   │   ├── main.tsx        # 入口
│   │   ├── App.tsx         # 根组件
│   │   ├── i18n.tsx        # 国际化
│   │   ├── authSession.ts  # 认证会话管理
│   │   ├── pages/          # 页面组件
│   │   │   ├── Accounts.tsx     # 账户管理
│   │   │   ├── Sites.tsx        # 站点管理
│   │   │   ├── Tokens.tsx       # 令牌管理
│   │   │   ├── TokenRoutes.tsx  # 令牌路由决策
│   │   │   ├── Dashboard.tsx    # 仪表盘
│   │   │   ├── ModelTester.tsx  # 模型测试
│   │   │   ├── ProxyLogs.tsx    # 代理日志
│   │   │   ├── Settings.tsx     # 系统设置
│   │   │   ├── OAuthManagement.tsx # OAuth 管理
│   │   │   └── ...更多
│   │   ├── components/    # 可复用组件
│   │   │   ├── Mobile*.tsx       # 移动端自适应组件
│   │   │   ├── Responsive*.tsx   # 响应式布局组件
│   │   │   ├── BrandIcon.tsx     # 品牌图标
│   │   │   ├── SearchModal.tsx   # 搜索弹窗
│   │   │   └── ...
│   │   └── charts/        # 图表组件 (VChart)
│   │
│   ├── desktop/           # Electron 桌面端
│   │   ├── main.ts        # 主进程
│   │   ├── runtime.ts     # 运行时管理
│   │   └── navigationGuard.ts # 导航守卫
│   │
│   └── shared/            # 前后端共享类型与校验
│       ├── platformIdentity.*     # 平台身份识别
│       ├── tokenRouteContract.*   # 令牌路由契约
│       ├── proxyLogMeta.*         # 代理日志元数据
│       └── ...
│
├── drizzle/               # 数据库迁移文件
├── docs/                  # VitePress 文档站
├── deploy/                # 部署配置 (K3s)
├── docker/                # Docker 相关
├── scripts/               # 开发/构建脚本
├── build/                 # 构建输出
├── data/                  # 运行时数据目录
│
├── package.json
├── tsconfig.json           # 主 TypeScript 配置
├── tsconfig.server.json    # 服务端专用配置
├── tsconfig.web.json       # Web 前端配置
├── tsconfig.desktop.json   # 桌面端配置
├── vite.config.ts          # Vite 构建配置
├── vitest.config.ts        # Vitest 测试配置
├── drizzle.config.ts       # Drizzle ORM 配置
├── electron-builder.yml    # Electron 打包配置
└── .env.example            # 环境变量模板
```

---

## 架构分层

```
┌─────────────────────────────────────────────────────────────┐
│                   下游客户端 (Cursor/Claude Code/Open WebUI)     │
└──────────────────┬──────────────────────────────────────────┘
                   │ OpenAI/Anthropic 兼容 API
┌──────────────────▼──────────────────────────────────────────┐
│              Proxy Routes (src/server/routes/proxy/)          │
│   /v1/chat/completions  /v1/models  /v1/completions ...      │
└──────────────────┬──────────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────────┐
│                   Proxy Core Orchestration                    │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────┐    │
│  │  Executors  │  │  Conductor   │  │    Surfaces      │    │
│  │ (Claude/    │──│ (Retry/Flow) │──│ (Chat/Files/     │    │
│  │  Codex/...) │  │              │  │  Models/...)     │    │
│  └─────────────┘  └──────────────┘  └──────────────────┘    │
└──────────────────┬──────────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────────┐
│                Transformers (协议转换层)                       │
│   OpenAI ⇄ Canonical ⇄ Anthropic / Gemini / ...              │
└──────────────────┬──────────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────────┐
│             Services (业务逻辑层)                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────────┐  │
│  │  Model   │ │TokenRouter│ │Platforms │ │ OAuth/Auth    │  │
│  │ Service  │ │ Service  │ │Adapter   │ │ Service       │  │
│  └──────────┘ └──────────┘ └──────────┘ └───────────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────────┐  │
│  │  Proxy   │ │ Billing  │ │  Stats   │ │ Scheduler     │  │
│  │  Logging │ │ Service  │ │ Service  │ │ Service       │  │
│  └──────────┘ └──────────┘ └──────────┘ └───────────────┘  │
└──────────────────┬──────────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────────┐
│              DB Layer (Drizzle ORM + SQLite/MySQL/Postgres)  │
└──────────────────┬──────────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────────┐
│              上游 AI 中转站 (New API/One API/Claude/... )      │
└─────────────────────────────────────────────────────────────┘
```

---

## 核心模块

### 1. API 管理 (Management API)

`src/server/routes/api/` — 提供 RESTful 管理接口：

| 路由文件 | 功能 |
|---------|------|
| `accounts.ts` | 账户 CRUD、登录验证、健康检测 |
| `sites.ts` | 站点 CRUD、代理配置、自动检测 |
| `tokens.ts` | 下游 API 令牌管理 |
| `accountTokens.ts` | 账户级令牌配置与同步 |
| `stats.ts` | 仪表盘统计、市场数据、日志查询 |
| `auth.ts` | 管理员认证 |
| `settings.ts` | 系统设置、数据库迁移、导出导入 |
| `oauth.ts` | OAuth 登录注册与配置 |
| `checkin.ts` | 自动签到控制 |
| `downstreamApiKeys.ts` | 下游 API Key 管理 |
| `test.ts` | 代理连通性测试 |
| `updateCenter.ts` | 更新中心管理 |
| `events.ts` | 服务端事件推送 |
| `monitor.ts` | 监控状态 API |

### 2. 代理路由 (Proxy Routes)

`src/server/routes/proxy/` — 兼容 OpenAI API 格式的代理端点：

| 路由文件 | 对应端点 |
|---------|---------|
| `chat.ts` | `/v1/chat/completions` |
| `completions.ts` | `/v1/completions` |
| `embeddings.ts` | `/v1/embeddings` |
| `models.ts` | `/v1/models` |
| `images.ts` | `/v1/images/*` |
| `responses.ts` | `/v1/responses` |
| `files.ts` | `/v1/files/*` |
| `gemini.ts` | `/v1/{model}:generateContent` |
| `search.ts` | `/v1/search` (Web Search) |
| `videos.ts` | `/v1/videos/*` (视频生成) |
| `endpointFlow.ts` | 端点自动降级与备份流 |

### 3. 代理核心 (Proxy Core)

`src/server/proxy-core/` — AI 代理的执行引擎：

- **`orchestration/endpointFlow.ts`** — 端到端请求编排，支持端点失败回退
- **`orchestration/upstreamRequest.ts`** — 上游请求构建与发送
- **`conductor/DefaultProxyConductor.ts`** — 代理执行指挥器，协调整个请求生命周期
- **`conductor/retryPolicy.ts`** — 智能重试策略
- **`executors/`** — 各协议专用执行器（Claude Code、Codex、Gemini CLI）
- **`providers/`** — 供应商注册表与配置文件
- **`surfaces/`** — API 协议表面实现（聊天、文件、模型列表等）
- **`runtime/`** — Codex WebSocket 会话、SSE 序列化
- **`cliProfiles/`** — CLI 工具的兼容配置适配
- **`capabilities/`** — 功能特性声明（对话文件、Responses compact）

### 4. 平台适配器 (Platform Adapters)

`src/server/services/platforms/` — 上游中转站的适配实现：

- `newApi.ts` — New API 适配
- `oneApi.ts` — One API 适配
- `oneHub.ts` — OneHub 适配
- `doneHub.ts` — DoneHub 适配
- `veloera.ts` — Veloera 适配
- `anyrouter.ts` — AnyRouter 适配
- `sub2api.ts` — Sub2API 适配
- `claude.ts` — Claude 官方适配
- `codex.ts` — Codex CLI 适配
- `gemini.ts` — Google Gemini 适配
- `geminiCli.ts` — Gemini CLI 适配
- `antigravity.ts` — Antigravity 适配
- `openai.ts` — OpenAI 官方适配

### 5. OAuth 认证

`src/server/services/oauth/` — 支持以下服务的 OAuth 登录：

- Claude (Anthropic)
- Codex (OpenAI)
- Gemini CLI (Google)
- Antigravity

### 6. 数据库层

- **ORM**: Drizzle ORM，支持 SQLite/MySQL/PostgreSQL
- **Schema 管理**: 基于 TypeScript 的 schema 定义，自动生成迁移
- **运行时引导**: 支持多数据库方言的运行时 schema 初始化
- **契约生成**: schema 契约自动生成和校验

### 7. 前端管理面板

**页面一览**：

| 页面 | 功能 |
|------|------|
| **Dashboard** | 仪表盘概览，显示站点状态、用量统计 |
| **Accounts** | 上游中转站账户管理、连接状态 |
| **Sites** | 上游站点配置和管理 |
| **Tokens** | 下游 API Key 分发与控制 |
| **Token Routes** | 令牌路由决策可视化 |
| **Model Tester** | 在线模型对话测试 |
| **Proxy Logs** | 代理请求日志查看与检索 |
| **Settings** | 系统设置、通知规则、路由策略 |
| **OAuth Management** | OAuth 账号绑定与管理 |
| **Checkin Log** | 签到日志查看 |
| **Notifications** | 通知配置 |

---

## 支持的平台

Metapi 可以代理和聚合以下平台的 API：

| 平台类型 | 具体平台 |
|---------|---------|
| **中转站面板** | New API、One API、OneHub、DoneHub、Veloera、AnyRouter、Sub2API |
| **官方 API** | OpenAI、Anthropic Claude、Google Gemini |
| **CLI 工具** | Codex CLI、Gemini CLI、Claude Code |

支持多种上游 API 协议探测方式（标准 API、管理 API、图形 API 等），并自动识别平台类型。

---

## 快速开始

### 环境要求

- Node.js >= 25.0.0
- npm / pnpm

### 本地开发

```bash
# 1. 克隆并安装依赖
git clone https://github.com/cita-777/metapi.git
cd metapi
npm install

# 2. 复制环境变量并编辑
cp .env.example .env
# 修改 AUTH_TOKEN、ACCOUNT_CREDENTIAL_SECRET 等配置

# 3. 初始化数据库
npm run db:migrate

# 4. 启动开发服务 (后端 + 前端)
npm run dev

# 5. 浏览器访问 http://localhost:5173
# 使用 .env 中设置的 AUTH_TOKEN 登录
```

### 生产构建

```bash
# 构建所有模块
npm run build

# 启动服务
npm start
```

### 桌面端

```bash
# 开发模式 (Electron + 后端 + 前端)
npm run dev:desktop

# 打包桌面应用
npm run package:desktop
```

### Docker

```bash
docker run -d \
  --name metapi \
  -p 4000:4000 \
  -v $(pwd)/data:/app/data \
  -e AUTH_TOKEN=your-admin-token \
  1467078763/metapi:latest
```

---

## 部署方式

Metapi 支持多种部署方式：

| 方式 | 文档链接 |
|------|---------|
| **Docker** | [部署指南](https://metapi.cita777.me/deployment) |
| **Render** | 一键部署按钮 (README 中) |
| **Zeabur** | 一键部署按钮 (README 中) |
| **K3s** | `deploy/k3s/` 目录下提供 K8s 配置 |
| **Electron 桌面版** | Windows/macOS 桌面安装包 |

---

## 测试

```bash
# 运行所有测试
npm test

# Schema 相关测试
npm run test:schema:unit
npm run test:schema:parity

# 数据库冒烟测试
npm run smoke:db:sqlite
npm run smoke:db:mysql
npm run smoke:db:postgres

# 架构边界检查
npm run repo:drift-check
```

---

## 配置参考

核心环境变量 (`.env`)：

| 变量 | 说明 |
|------|------|
| `AUTH_TOKEN` | 管理员登录令牌（必填） |
| `ACCOUNT_CREDENTIAL_SECRET` | 账户凭据加密密钥（必填，32+ 字节随机串） |
| `PROXY_TOKEN` | 代理接口认证令牌 |
| `PORT` | 服务端口（默认 4000） |
| `DATA_DIR` | 数据存储目录（默认 `./data`） |
| `DB_TYPE` | 数据库类型: `sqlite` / `mysql` / `postgres` |
| `SYSTEM_PROXY_URL` | 系统代理 URL |
| `CHECKIN_CRON` | 自动签到 cron 表达式 |
| `WEBHOOK_URL` | 通知 Webhook |
| `TZ` | 时区（默认 Asia/Shanghai） |

完整配置说明见[在线文档](https://metapi.cita777.me/configuration)。

---

## 相关链接

- **在线文档**: https://metapi.cita777.me
- **GitHub**: https://github.com/cita-777/metapi
- **Docker Hub**: https://hub.docker.com/r/1467078763/metapi
- **在线体验**: https://metapi-t9od.onrender.com (管理员令牌: `123456`)
